# v4.8.22 多配置仪表盘、主题与真实连接验收报告

## 验收范围

- 模块 1：配置 UUID、稳定唯一网卡名、旧配置迁移和 Windows Wintun 精确清理回归。
- 模块 2：仪表盘多配置面板、拖拽排序、逐项连接/断开/编辑、连接全部、错误隔离和状态刷新。
- 模块 3：配置管理卡片浅色、深色和自定义主题适配。
- 模块 4：版本一致性、Windows 真实双配置生命周期、Windows/Android 正式构建、签名和哈希。

## 自动化回归

| 项目 | 结果 |
| --- | --- |
| `flutter analyze` | 0 问题 |
| `flutter test` | 242 项全部通过 |
| Rust `cargo check` | 通过，仅保留既有警告 |
| `git diff --check` | 无新增空白错误 |
| 版本一致性 | `4.8.22+40822` |

多配置专项覆盖了面板卡片、排序回调、逐项操作、批量结果、连接监听和 Android/iOS 单 VPN 平台判断；主题专项覆盖浅色、深色和自定义 `ColorScheme`。

## Windows 真实 UI 可见性

- 从隔离的系统临时目录启动正式便携版 `vnt_app.exe`，进程路径指向验收副本，窗口标题为 `VNTC APP2.0 v4.8.22`。
- 可访问性树真实读取到：`2 个配置`、`已添加 2 个配置，可一键连接全部`、`连接全部配置`。
- 当前 Windows 图形捕获后端对该无边框 Flutter 窗口返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`，因此没有使用不受支持的坐标点击绕过；真实窗口只做可见性读取，交互行为由 Flutter 自动化测试验证。

## Windows 真实双配置网络验收

验收使用系统临时目录中的两份专用配置；仅复用原配置的有效服务器和令牌，另设独立 UUID、设备 ID、设备名、自动虚拟 IP 和唯一网卡名。安装目录中的用户配置数量保持为 1，验收结果不记录令牌或服务器内容。

### 同时连接

| 配置 | 网卡 | 状态 | 接口索引 | 接口 GUID | Rust 状态 |
| --- | --- | --- | ---: | --- | --- |
| 验收配置 A | `VNT-App-TUN-cfg-60d6122e5c690abd` | Up | 53 | `{F4047A5E-0041-407F-810D-41BBFA249C21}` | Online |
| 验收配置 B | `VNT-App-TUN-cfg-68240f2caec2d877` | Up | 42 | `{79A74DA5-E79C-4A88-A5C4-CE65F6A19D18}` | Online |

- `VntManager.size()` 为 2。
- 两个实例均取得非空虚拟 IP，并均连接到服务器。

### 精确删除与独立断开

1. 对配置 A 调用与配置删除流程相同的 `VirtualNetworkAdapterManager.deleteForConfig`。
2. 配置 A 的 Wintun 精确消失；配置 B 仍为 `Up`，接口 GUID 保持 `{79A74DA5-E79C-4A88-A5C4-CE65F6A19D18}`，连接实例仍存在。
3. 断开配置 A 后，`VntManager.size()` 为 1；配置 B 仍连接。
4. 断开并清理配置 B 后，`VntManager.size()` 为 0，两张验收网卡均不存在。
5. 验收退出的 `finally` 再次按两个精确配置名执行兜底清理，最终系统中无验收网卡残留。

结构化原始结果生成于隔离临时目录的 `acceptance-result.json`，其中 `success=true`；清理临时目录前已将上述非敏感证据写入本报告。

## Android 正式母版验收

| 项目 | 结果 |
| --- | --- |
| 包名 | `top.wherewego.vnt_app` |
| 版本 | `versionName=4.8.22`、`versionCode=40822` |
| ABI | 仅 `arm64-v8a` |
| 16KB 对齐 | `zipalign -c -P 16 -v 4` 通过 |
| APK 签名 | v2=true、v3=true |
| 官方证书 SHA-256 | `83EF92B147643697EA8EFAF4B73A9D370875EE0FD1CBA10A7AC03E184F44D8C7` |
| APK SHA-256 | `CFE4AC10FC8AE53C1466F6A1B558871C62DF9A88642A359508B0373DE3F5E299` |
| SHA-256 旁车 | 匹配 |

## 正式构建产物

| 平台 | 文件 | 字节数 | SHA-256 |
| --- | --- | ---: | --- |
| Windows 安装包 | `release/installer/VNT_App_4.8.22_Windows_Setup.exe` | 51,072,333 | `B7432131B4472AB25737BC3B6500B1D1999AFB23EC943C1288194810D3619384` |
| Windows 便携包 | `release/portable/VNT_App_4.8.22_Windows_Portable.zip` | 48,455,010 | `93F1AC6042F8B60E77424A0D4AFB2FF1E15DD3CA2C3E884B91008158CCA0CD2F` |
| Android arm64 品牌母版 | `release/android/VNT_App_4.8.22_Android_arm64_Brand_Master.apk` | 68,574,241 | `CFE4AC10FC8AE53C1466F6A1B558871C62DF9A88642A359508B0373DE3F5E299` |

Windows 构建目录、输出目录和便携包内 `vnt_app.exe` 的 PE `FileVersion/ProductVersion` 均为 `4.8.22`；三份正式产物的 SHA-256 旁车均匹配。

## 平台约束结论

- Windows、macOS、Linux：允许多配置并发连接。
- Android、iOS：受系统单 VPN 通道限制，只允许一个活动配置；界面明确提示该限制，没有将单通道行为描述为真正多 VPN 并发。

## 验收结论

v4.8.22 的多配置控制、唯一网卡、精确清理和主题适配满足验收条件；Windows 双配置真实连接与独立清理成功，Windows/Android 正式产物及旁车校验通过。
